# Runtime dependencies.
# Last version to support python 2.5
zope.interface==3.8.0

twisted==12.1.0-chevah3
# Required by twisted.conch
pyasn1

simplejson
bunch

lazr.delegates==1.2.0

# This is here to avoid removing the chevah namespace package at dist time.
chevah-ftplib>=2.7.3-chevah3

# Required by documentation generation and as a generic template engine.
jinja2
pygments

# Required for testaccount on Windows.
unidecode
